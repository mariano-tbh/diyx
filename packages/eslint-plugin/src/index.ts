// eslint-plugin-diyx — placeholder
// Future rules:
//   - no-signal-after-await: warn when a signal is read after an await in an async generator
//   - no-signal-by-value: warn when a signal's .get() result is passed as a JSX prop instead of the signal itself

export const rules = {}

export default { rules }
