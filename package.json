{
  "name": "diyx",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "workspaces": [
    "packages/*",
    "app",
    "benchmarks"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=app",
    "build": "npm run build --workspace=app",
    "typecheck": "npm run typecheck --workspace=packages/lib && npm run typecheck --workspace=packages/eslint-plugin && npm run typecheck --workspace=app"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
